# T028.1 - Electron Builder Configuration Log

**Task**: Configure electron-builder for Windows packaging
**Date**: 2025-12-18
**Status**: Completed

## Files Modified

### 1. `desktop-app/electron-builder.json` (modified)
- Added `asarUnpack` configuration for native modules
- Added `npmRebuild: true` for native module compilation
- Existing NSIS configuration verified

## Configuration Summary

### Core Settings
| Setting | Value |
|---------|-------|
| App ID | com.contpaq.win |
| Product Name | ContPAQ Win |
| ASAR | Enabled |
| Compression | Maximum |
| Target | NSIS (x64) |

### Native Module Handling
```json
"asarUnpack": [
  "**/*.node",
  "**/better-sqlite3/**"
],
"npmRebuild": true
```

Native modules like `better-sqlite3` must be:
1. Rebuilt for Electron's Node.js version
2. Unpacked from ASAR archive (can't load .node from asar)

### NSIS Installer Settings
| Setting | Value |
|---------|-------|
| One-Click | false (customizable) |
| Per Machine | true (all users) |
| Allow Elevation | true |
| Custom Install Dir | true |
| Desktop Shortcut | true |
| Start Menu Shortcut | true |
| Language | 1034 (Spanish) |

### Code Signing
- `publisherName` configured for unsigned builds
- `forceCodeSigning` not set (allows dev builds without cert)
- Certificate can be provided via environment variables:
  - `CSC_LINK`: Path to .p12/.pfx certificate
  - `CSC_KEY_PASSWORD`: Certificate password

## Build Commands

### Development Build
```bash
cd desktop-app
npm run build
npm run package
```

### Production Build with Signing
```bash
# Set environment variables for code signing
export CSC_LINK=/path/to/certificate.p12
export CSC_KEY_PASSWORD=your_password

npm run package
```

### Output Location
- Installer: `desktop-app/release/ContPAQ Win-{version}-Setup.exe`

## Tests

- Test file: `tests/desktop_app/test_T028_1_electron_builder.py`
- Total: 28 tests passed

## Notes

- Code signing is optional for development builds
- Native modules require rebuild for Electron ABI compatibility
- Spanish language (LCID 1034) used for installer UI
- AI service bundled as extra resource
