# T015.1 Learning Log: AI Service API Client

## Date: 2025-12-18

## Key Learnings

### 1. TypeScript Fetch API with FormData
Sending files via multipart/form-data:

```typescript
async extractInvoice(file: File): Promise<InvoiceExtraction> {
  const formData = new FormData();
  formData.append('file', file, file.name);

  const response = await fetch(`${this.baseUrl}/extract`, {
    method: 'POST',
    body: formData,
    // Note: Don't set Content-Type header - browser sets it with boundary
  });

  return response.json();
}
```

### 2. Request Timeout with AbortController
Implementing timeout for fetch requests:

```typescript
async fetchWithTimeout(url: string, options: RequestInit, timeout: number) {
  const controller = new AbortController();
  const timeoutId = setTimeout(() => controller.abort(), timeout);

  try {
    const response = await fetch(url, {
      ...options,
      signal: controller.signal,
    });
    return response;
  } finally {
    clearTimeout(timeoutId);
  }
}
```

### 3. Retry Logic with Exponential Backoff
Implementing retry for transient failures:

```typescript
async fetchWithRetry(url: string, options: RequestInit): Promise<Response> {
  for (let attempt = 1; attempt <= this.maxRetries; attempt++) {
    try {
      const response = await fetch(url, options);

      if (response.ok) return response;

      // Only retry on 5xx errors
      if (response.status >= 500 && attempt < this.maxRetries) {
        await delay(Math.pow(2, attempt) * 100); // 200ms, 400ms, 800ms
        continue;
      }

      throw new Error(`Request failed: ${response.status}`);
    } catch (error) {
      if (attempt >= this.maxRetries) throw error;
      await delay(Math.pow(2, attempt) * 100);
    }
  }
}
```

### 4. Snake Case to Camel Case Conversion
Converting Python API responses to TypeScript conventions:

```typescript
function convertExtractionField(field: ApiExtractionField): ExtractionField {
  return {
    fieldName: field.field_name,    // snake_case → camelCase
    value: field.value,
    confidence: Math.round(field.confidence * 100), // 0-1 → 0-100
    userVerified: field.user_verified,
    bbox: field.bbox,
  };
}
```

### 5. Custom Error Classes
Creating typed errors for better error handling:

```typescript
export class AIServiceError extends Error {
  public readonly statusCode?: number;
  public readonly detail?: string;

  constructor(message: string, statusCode?: number, detail?: string) {
    super(message);
    this.name = 'AIServiceError';
    this.statusCode = statusCode;
    this.detail = detail;
  }
}

// Usage
try {
  await client.extractInvoice(file);
} catch (error) {
  if (error instanceof AIServiceError) {
    if (error.statusCode === 400) {
      // Handle invalid PDF
    } else if (error.statusCode === 413) {
      // Handle file too large
    }
  }
}
```

### 6. Mocking Fetch in Jest
Setting up fetch mocks for testing:

```typescript
const mockFetch = jest.fn();
global.fetch = mockFetch;

beforeEach(() => {
  jest.clearAllMocks();
});

it('should handle success', async () => {
  mockFetch.mockResolvedValueOnce({
    ok: true,
    json: () => Promise.resolve({ data: 'test' }),
  });

  const result = await client.someMethod();
  expect(mockFetch).toHaveBeenCalledWith(url, options);
});

it('should handle network errors', async () => {
  mockFetch.mockRejectedValueOnce(new Error('Network error'));
  await expect(client.someMethod()).rejects.toThrow('Network error');
});
```

### 7. Testing Retry Behavior
Verifying retry logic works correctly:

```typescript
it('should retry on network failure', async () => {
  // Fail twice, then succeed
  mockFetch
    .mockRejectedValueOnce(new Error('Network error'))
    .mockRejectedValueOnce(new Error('Network error'))
    .mockResolvedValueOnce({
      ok: true,
      json: () => Promise.resolve(successResponse),
    });

  const result = await client.extractInvoice(file);

  expect(mockFetch).toHaveBeenCalledTimes(3);
  expect(result).toBeDefined();
});
```

### 8. Graceful Health Check
Health check that never throws:

```typescript
async checkHealth(): Promise<ServiceHealth> {
  try {
    const response = await fetch(`${this.baseUrl}/health`);

    if (!response.ok) {
      return {
        status: 'error',
        healthy: false,
        message: `Service returned ${response.status}`,
        timestamp: new Date().toISOString(),
      };
    }

    return await response.json();
  } catch (error) {
    // Network failure - service unreachable
    return {
      status: 'stopped',
      healthy: false,
      message: error.message,
      timestamp: new Date().toISOString(),
    };
  }
}
```

### 9. Handling Optional/Null Fields
Converting null API fields to empty defaults:

```typescript
function convertExtractionResponse(response: ApiResponse): InvoiceExtraction {
  return {
    vendorRfc: convertField(response.vendor_rfc) ?? createEmptyField('vendor_rfc'),
    // ... other fields
  };
}

function createEmptyField(fieldName: string): ExtractionField {
  return {
    fieldName,
    value: '',
    confidence: 0,
    userVerified: false,
  };
}
```

### 10. Type-Safe API Client Pattern
Strongly typed API responses:

```typescript
// Define API response types (snake_case from Python)
interface ApiExtractionResponse {
  source_file: string;
  vendor_rfc?: ApiExtractionField | null;
  processing_time_ms: number;
}

// Define TypeScript types (camelCase)
interface InvoiceExtraction {
  sourceFile: string;
  vendorRfc: ExtractionField;
  processingTimeMs: number;
}

// Client converts between them
class AIServiceClient {
  async extractInvoice(file: File): Promise<InvoiceExtraction> {
    const response = await this.fetch<ApiExtractionResponse>('/extract');
    return convertExtractionResponse(response);
  }
}
```

## Files Referenced
- `desktop-app/src/renderer/services/ai-service.ts`
- `desktop-app/tests/renderer/services/ai-service.test.ts`
- `desktop-app/src/renderer/types/index.ts`

## Related Documentation
- [MDN Fetch API](https://developer.mozilla.org/en-US/docs/Web/API/Fetch_API)
- [MDN FormData](https://developer.mozilla.org/en-US/docs/Web/API/FormData)
- [MDN AbortController](https://developer.mozilla.org/en-US/docs/Web/API/AbortController)
- [Jest Mock Functions](https://jestjs.io/docs/mock-functions)
