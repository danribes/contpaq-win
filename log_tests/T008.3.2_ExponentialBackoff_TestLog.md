# T008.3.2 Test Log: Exponential Backoff

## Test Overview
- **Task ID**: T008.3.2
- **Test File**: `desktop-app/tests/main/process-manager.test.ts`
- **New Tests Added**: 10
- **Total Tests**: 275 (265 previous + 10 new)
- **Framework**: Jest, ts-jest
- **Date**: 2025-12-17

## Test Categories

### Backoff Configuration (3 tests)
| Test | Description |
|------|-------------|
| should have restartBackoffMs configuration option | Config property exists |
| should default restartBackoffMs to 1000 | Default 1s base delay |
| should allow custom restartBackoffMs via config | Custom configuration |

### Exponential Delay Pattern (4 tests)
| Test | Description |
|------|-------------|
| should delay first restart by base backoff (1s) | 1st restart = 1000ms |
| should delay second restart by 2x backoff (2s) | 2nd restart = 2000ms |
| should delay third restart by 4x backoff (4s) | 3rd restart = 4000ms |
| should include delay in restart event | Event includes delay field |

### Backoff Reset (1 test)
| Test | Description |
|------|-------------|
| should reset backoff delay after successful start period | Reset when counter reset |

### Bridge Service Backoff (2 tests)
| Test | Description |
|------|-------------|
| should apply exponential backoff to Bridge service | Bridge uses backoff |
| should track Bridge backoff independently from AI | Independent tracking |

## Test Execution

```bash
npm test -- --testPathPattern="process-manager" --testNamePattern="T008.3.2"
```

## Results

```
Test Suites: 1 passed, 1 total
Tests:       275 passed, 275 total
Time:        13.88 s
```

## Key Test Patterns

### Using Fake Timers for Backoff Tests

```typescript
beforeEach(() => {
  jest.clearAllMocks();
  jest.useFakeTimers();
});

afterEach(() => {
  jest.useRealTimers();
});
```

### Testing First Restart Delay (1s)

```typescript
it('should delay first restart by base backoff (1s)', async () => {
  manager = new ProcessManager({
    enableAutoRestart: true,
    restartBackoffMs: 1000,
  });

  // ... setup mock spawn ...

  // Start service
  const startPromise = manager.startAIService();
  jest.advanceTimersByTime(10);
  await startPromise;
  expect(spawnCount).toBe(1);

  // Crash the service
  processes[0].emit('exit', 1, null);

  // Should not restart immediately
  jest.advanceTimersByTime(500);
  expect(spawnCount).toBe(1);

  // Should restart after 1000ms
  jest.advanceTimersByTime(600);
  expect(spawnCount).toBe(2);
});
```

### Testing Second Restart Delay (2s)

```typescript
it('should delay second restart by 2x backoff (2s)', async () => {
  manager = new ProcessManager({
    enableAutoRestart: true,
    restartBackoffMs: 1000,
    maxRestarts: 5,
  });

  // ... start service ...

  // First crash and restart (1s delay)
  processes[0].emit('exit', 1, null);
  jest.advanceTimersByTime(1100);
  expect(spawnCount).toBe(2);

  // Wait for spawn event
  jest.advanceTimersByTime(10);

  // Second crash
  processes[1].emit('exit', 1, null);

  // Should not restart after 1s
  jest.advanceTimersByTime(1500);
  expect(spawnCount).toBe(2);

  // Should restart after 2s total
  jest.advanceTimersByTime(600);
  expect(spawnCount).toBe(3);
});
```

### Testing Delay in Restart Event

```typescript
it('should include delay in restart event', async () => {
  manager = new ProcessManager({
    enableAutoRestart: true,
    restartBackoffMs: 1000,
  });

  const listener = jest.fn();
  manager.on('restart', listener);

  // ... start and crash service ...

  expect(listener).toHaveBeenCalledWith(
    expect.objectContaining({
      service: 'ai',
      delay: 1000,
    })
  );
});
```

### Testing Independent Service Tracking

```typescript
it('should track Bridge backoff independently from AI', async () => {
  manager = new ProcessManager({
    enableAutoRestart: true,
    restartBackoffMs: 1000,
    maxRestarts: 10,
  });

  // Start both services
  await manager.startAIService();
  await manager.startBridgeService();

  // Crash AI twice to increase its backoff
  processes[0].emit('exit', 1, null);
  jest.advanceTimersByTime(1100);
  processes[2].emit('exit', 1, null);
  jest.advanceTimersByTime(2100);

  // Bridge crashes for first time - should use base backoff (1s)
  processes[1].emit('exit', 1, null);
  jest.advanceTimersByTime(1100);

  // Bridge should have restarted after 1s, not 4s
  expect(bridgeSpawnCount).toBe(2);
});
```

## Notes

1. **Fake Timers Essential**: Jest fake timers required for testing delays without real waits

2. **Time Advancement Pattern**: Use `jest.advanceTimersByTime()` to simulate delay passing

3. **Spawn Event Timing**: Remember to advance time for spawn event (10ms in tests)

4. **Independent Service Tests**: Verify AI backoff doesn't affect Bridge and vice versa

5. **Updated T008.3.1 Tests**: Added `restartBackoffMs: 10` to existing auto-restart tests for speed
