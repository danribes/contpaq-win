# T008.1.5 Test Log: restartAIService Enhanced

## Test Overview
- **Task ID**: T008.1.5
- **Test File**: `desktop-app/tests/main/process-manager.test.ts`
- **New Tests Added**: 13
- **Total Tests**: 137 (124 previous + 13 new)
- **Framework**: Jest, ts-jest
- **Date**: 2025-12-17

## Test Categories

### Restart Counter (4 tests)
| Test | Description |
|------|-------------|
| should increment restart counter when restarting | Counter increments on restart |
| should increment restart counter multiple times | Counter tracks multiple restarts |
| should not increment restart counter if service was not running | No increment for initial start |
| should be reset by resetRestartCount | Counter can be reset to 0 |

### Restart Event (3 tests)
| Test | Description |
|------|-------------|
| should emit restart event when restarting a running service | Event emitted on restart |
| should not emit restart event for initial start | No event for initial start |
| should include restart count in event | Event contains restartCount |

### Uptime Reset (1 test)
| Test | Description |
|------|-------------|
| should reset uptime after restart | Uptime resets after restart |

### Error Handling (3 tests)
| Test | Description |
|------|-------------|
| should propagate error if start fails during restart | Errors are propagated |
| should still increment restart counter if start fails | Counter increments even on failure |
| should set lastError if restart fails | lastError is set on failure |

### Status Transitions (2 tests)
| Test | Description |
|------|-------------|
| should transition through correct states during restart | Correct status flow |
| should end in RUNNING state after successful restart | Final state is RUNNING |

## Test Execution

```bash
npm test -- --testPathPattern="process-manager" --testNamePattern="T008.1.5"
```

## Results

```
Test Suites: 1 passed, 1 total
Tests:       137 passed, 137 total
Time:        8.444 s
```

## Key Test Patterns

### Testing Restart Counter Increment
```typescript
it('should increment restart counter when restarting', async () => {
  await startService();
  setupGracefulStop();

  expect(manager.getRestartCount('ai')).toBe(0);

  await manager.restartAIService();

  expect(manager.getRestartCount('ai')).toBe(1);
});
```

### Testing Restart Event Emission
```typescript
it('should emit restart event when restarting a running service', async () => {
  await startService();
  setupGracefulStop();

  const restartListener = jest.fn();
  manager.on('restart', restartListener);

  await manager.restartAIService();

  expect(restartListener).toHaveBeenCalled();
  expect(restartListener).toHaveBeenCalledWith(
    expect.objectContaining({
      service: 'ai',
    })
  );
});
```

### Testing No Event for Initial Start
```typescript
it('should not emit restart event for initial start', async () => {
  const restartListener = jest.fn();
  manager.on('restart', restartListener);

  expect(manager.getAIServiceStatus()).toBe(ServiceStatus.STOPPED);
  await manager.restartAIService();

  // Should not emit restart for initial start
  expect(restartListener).not.toHaveBeenCalled();
});
```

### Testing Error Propagation
```typescript
it('should propagate error if start fails during restart', async () => {
  await startService();
  setupGracefulStop();

  // Setup start to fail on next call
  mockSpawn.mockImplementation(() => {
    const failProcess = createMockChildProcess();
    setTimeout(() => failProcess.emit('error', new Error('Start failed')), 10);
    return failProcess as never;
  });

  await expect(manager.restartAIService()).rejects.toThrow('Start failed');
  expect(manager.getAIServiceStatus()).toBe(ServiceStatus.ERROR);
});
```

## Helper Functions Used

```typescript
// Helper to start the service
async function startService(): Promise<void> {
  await manager.startAIService();
  expect(manager.getAIServiceStatus()).toBe(ServiceStatus.RUNNING);
}

// Helper to setup kill mock for graceful stop
function setupGracefulStop(): void {
  mockProcess.kill.mockImplementation(() => {
    mockProcess.emit('exit', 0, 'SIGTERM');
    return true;
  });
}
```

## Notes

1. **Multiple Restarts Testing**: For tests that restart multiple times, a new mock process must be created for each restart since the original process is "killed".

2. **True Restart vs Initial Start**: Tests verify that restart counter and event are only triggered when the service was actually running before.

3. **Error Handling**: The counter is incremented before start is attempted, ensuring failed restarts are still counted for max retry logic.
