# T008.3.1 Test Log: Auto-Restart with Max Retries

## Test Overview
- **Task ID**: T008.3.1
- **Test File**: `desktop-app/tests/main/process-manager.test.ts`
- **New Tests Added**: 20
- **Total Tests**: 265 (245 previous + 20 new)
- **Framework**: Jest, ts-jest
- **Date**: 2025-12-17

## Test Categories

### Max Restarts Configuration (4 tests)
| Test | Description |
|------|-------------|
| should have maxRestarts configuration option | Config property exists |
| should default maxRestarts to 3 | Default value |
| should allow custom maxRestarts via config | Custom configuration |
| should allow zero maxRestarts (disabled) | Zero disables restarts |

### Auto-Restart on Crash (5 tests)
| Test | Description |
|------|-------------|
| should have enableAutoRestart configuration option | Config property exists |
| should default enableAutoRestart to true | Enabled by default |
| should auto-restart AI service when it crashes | Auto-restart behavior |
| should not auto-restart when disabled | Config respected |
| should not auto-restart on graceful stop (exit code 0) | Only crashes trigger |

### Max Restarts Limit (3 tests)
| Test | Description |
|------|-------------|
| should stop auto-restarting after max restarts reached | Limit enforced |
| should emit maxRestartsExceeded event when limit reached | Event emission |
| should include maxRestarts in maxRestartsExceeded event | Event data |

### Restart Counter for Auto-Restart (3 tests)
| Test | Description |
|------|-------------|
| should increment restart count on auto-restart | Counter increments |
| should track restart count across multiple crashes | Counter persists |
| should allow manual reset of restart count | resetRestartCount() works |

### Bridge Service Auto-Restart (3 tests)
| Test | Description |
|------|-------------|
| should auto-restart Bridge service when it crashes | Bridge auto-restart |
| should track Bridge restart count separately from AI | Independent counters |
| should emit maxRestartsExceeded for Bridge service | Bridge event |

### Restart Event Emission (2 tests)
| Test | Description |
|------|-------------|
| should emit restart event on auto-restart | Event emitted |
| should include reason in restart event | Crash reason included |

## Test Execution

```bash
npm test -- --testPathPattern="process-manager" --testNamePattern="T008.3.1"
```

## Results

```
Test Suites: 1 passed, 1 total
Tests:       265 passed, 265 total
Time:        13.82 s
```

## Key Test Patterns

### Testing Auto-Restart on Crash

```typescript
it('should auto-restart AI service when it crashes', async () => {
  const autoRestartManager = new ProcessManager({ enableAutoRestart: true });

  let spawnCount = 0;
  const processes: ReturnType<typeof createMockChildProcess>[] = [];

  mockSpawn.mockImplementation(() => {
    spawnCount++;
    const proc = createMockChildProcess();
    processes.push(proc);
    setTimeout(() => proc.emit('spawn'), 10);
    return proc as never;
  });

  await autoRestartManager.startAIService();
  expect(spawnCount).toBe(1);

  // Simulate crash (non-zero exit)
  processes[0].emit('exit', 1, null);

  // Wait for auto-restart
  await new Promise(resolve => setTimeout(resolve, 100));

  // Should have spawned a new process
  expect(spawnCount).toBeGreaterThan(1);
});
```

### Testing Max Restarts Limit

```typescript
it('should stop auto-restarting after max restarts reached', async () => {
  const limitedManager = new ProcessManager({
    enableAutoRestart: true,
    maxRestarts: 3,
  });

  let spawnCount = 0;
  const processes: ReturnType<typeof createMockChildProcess>[] = [];

  mockSpawn.mockImplementation(() => {
    spawnCount++;
    const proc = createMockChildProcess();
    processes.push(proc);
    setTimeout(() => proc.emit('spawn'), 10);
    return proc as never;
  });

  await limitedManager.startAIService();
  expect(spawnCount).toBe(1);

  // Crash 1-3: Should restart each time
  for (let i = 0; i < 3; i++) {
    processes[i].emit('exit', 1, null);
    await new Promise(resolve => setTimeout(resolve, 50));
  }
  expect(spawnCount).toBe(4);

  // Crash 4: Should NOT restart (max reached)
  processes[3].emit('exit', 1, null);
  await new Promise(resolve => setTimeout(resolve, 100));
  expect(spawnCount).toBe(4);
});
```

### Testing Event Emission with Reason

```typescript
it('should include reason in restart event', async () => {
  const autoRestartManager = new ProcessManager({ enableAutoRestart: true });

  const listener = jest.fn();
  autoRestartManager.on('restart', listener);

  const processes: ReturnType<typeof createMockChildProcess>[] = [];

  mockSpawn.mockImplementation(() => {
    const proc = createMockChildProcess();
    processes.push(proc);
    setTimeout(() => proc.emit('spawn'), 10);
    return proc as never;
  });

  await autoRestartManager.startAIService();

  processes[0].emit('exit', 1, 'SIGSEGV');
  await new Promise(resolve => setTimeout(resolve, 100));

  expect(listener).toHaveBeenCalledWith(
    expect.objectContaining({
      service: 'ai',
      reason: expect.stringContaining('crash'),
    })
  );
});
```

### Testing Independent Service Counters

```typescript
it('should track Bridge restart count separately from AI', async () => {
  const autoRestartManager = new ProcessManager({ enableAutoRestart: true });

  const processes: ReturnType<typeof createMockChildProcess>[] = [];

  mockSpawn.mockImplementation(() => {
    const proc = createMockChildProcess();
    processes.push(proc);
    setTimeout(() => proc.emit('spawn'), 10);
    return proc as never;
  });

  await autoRestartManager.startAIService();
  await autoRestartManager.startBridgeService();

  // Crash bridge only
  processes[1].emit('exit', 1, null);
  await new Promise(resolve => setTimeout(resolve, 50));

  expect(autoRestartManager.getRestartCount('ai')).toBe(0);
  expect(autoRestartManager.getRestartCount('bridge')).toBe(1);
});
```

## Notes

1. **Process Tracking Array**: Tests use an array to track multiple processes spawned during restarts

2. **Wait Times**: 50-100ms waits are needed for async restart operations

3. **Spawn Count Tracking**: `spawnCount` variable tracks how many processes were created

4. **Mock Implementation**: Each spawn creates a new mock process to simulate real behavior

5. **Independent Tests**: Each test creates its own manager with specific config
